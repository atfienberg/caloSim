#ifndef CONFIGURATION_H
#define CONFIGURATION_H

#include <string>

typedef struct CalorimeterConfiguration{
  

class RunConfiguration{
public:
  Configuration();
  Configuration(std::string);
  


#endif
